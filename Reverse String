class Solution {
    public String reverseString(String s) {
        if(s.length() == 0) {
            return "";
        }
        StringBuilder sb = new StringBuilder(s);
        return sb.reverse().toString();
    }
}
